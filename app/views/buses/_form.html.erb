<%= form_for(@bus) do |f| %>
  <% if @bus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bus.errors.count, "error") %> prohibited this bus from being saved:</h2>

      <ul>
      <% @bus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :leave_time %><br />
    <%= f.time_select :leave_time %>
  </div>
  
  <div class="field">
    <%= f.label :arrival_time %><br />
    <%= f.time_select :arrival_time %>
  </div>
  
  
  
 <div class="control-group">    
  <div class="field">
    <%= f.label :leaving_from_id, class: 'control-label' %><br />
	 <div class="controls">
	 	 <%= f.collection_select :leaving_from_id, @cities, :id, :name %>
  </div>
</div> 
 
<div class="control-group">  
  <div class="field">
    <%= f.label :traveling_to_id, class: 'control-label' %><br />
	<div class="controls">
	 <%= f.collection_select :traveling_to_id, @cities, :id, :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
